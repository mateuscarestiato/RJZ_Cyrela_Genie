# Databricks workspace URL
DATABRICKS_HOST=https://adb-3762468175228684.4.azuredatabricks.net

# OAuth bearer token or PAT
DATABRICKS_TOKEN=seu_token_aqui

# Genie Space ID (UUID)
GENIE_SPACE_ID=seu_space_id_aqui

# Optional tuning
GENIE_POLL_SECONDS=2
GENIE_TIMEOUT_SECONDS=600

# Supabase (Banco de Dados Cloud)
SUPABASE_URL=
SUPABASE_KEY=

# Chave de Criptografia (Fernet) para tokens
# Voce pode gerar uma rodando no python: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
ENCRYPTION_KEY=
