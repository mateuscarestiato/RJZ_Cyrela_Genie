Place your agent PNG here with this exact name:
agent_photo.png

When the app starts, it automatically crops the center area to a square and saves:
agent_avatar_square.png

The square image is then used as the assistant avatar in chat responses.
